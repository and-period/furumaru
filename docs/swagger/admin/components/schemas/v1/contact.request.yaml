updateContactRequest:
  type: object
  properties:
    status:
      $ref: './../../../openapi.yaml#/components/schemas/contactStatus'
    note:
      type: string
      description: 対応メモ(2000文字まで)
  required:
  - status
  - note
  example:
    status: 1
    note: '対応メモ'
