uploadCoordinatorThumbnailRequest:
  type: object
  properties:
    thumbnail:
      type: string
      format: binary
      description: 仲介者サムネイル(png,jpeg形式,10MBまで)
uploadCoordinatorHeaderRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: 仲介者ヘッダー画像(png,jpeg形式,10MBまで)
uploadProducerThumbnailRequest:
  type: object
  properties:
    thumbnail:
      type: string
      format: binary
      description: 生産者サムネイル(png,jpeg形式,10MBまで)
uploadProducerHeaderRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: 生産者ヘッダー画像(png,jpeg形式,10MBまで)
