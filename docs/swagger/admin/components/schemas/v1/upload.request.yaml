getUploadUrlRequest:
  type: object
  properties:
    fileType:
      type: string
      description: MIMEタイプ
  required:
  - fileType
  example:
    fileType: 'image/png'
uploadCoordinatorThumbnailRequest:
  type: object
  properties:
    thumbnail:
      type: string
      format: binary
      description: コーディネータサムネイル(png,jpeg形式,10MBまで)
uploadCoordinatorHeaderRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: コーディネータヘッダー画像(png,jpeg形式,10MBまで)
uploadCoordinatorPromotionVideoRequest:
  type: object
  properties:
    video:
      type: string
      format: binary
      description: コーディネータ紹介映像(mp4形式,200MBまで)
uploadCoordinatorBonusVideoRequest:
  type: object
  properties:
    video:
      type: string
      format: binary
      description: コーディネータ購入特典映像(mp4形式,200MBまで)
uploadProducerThumbnailRequest:
  type: object
  properties:
    thumbnail:
      type: string
      format: binary
      description: 生産者サムネイル(png,jpeg形式,10MBまで)
uploadProducerHeaderRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: 生産者ヘッダー画像(png,jpeg形式,10MBまで)
uploadProducerPromotionVideoRequest:
  type: object
  properties:
    video:
      type: string
      format: binary
      description: 生産者紹介映像(mp4形式,200MBまで)
uploadProducerBonusVideoRequest:
  type: object
  properties:
    video:
      type: string
      format: binary
      description: 生産者購入特典映像(mp4形式,200MBまで)
uploadProductImageRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: 商品画像(png,jpeg形式,10MBまで)
uploadProductVideoRequest:
  type: object
  properties:
    video:
      type: string
      format: binary
      description: 商品動画(mp4形式,200MBまで)
uploadProductTypeIconRequest:
  type: object
  properties:
    icon:
      type: string
      format: binary
      description: 商品アイコン(png,jpeg形式,10MBまで)
uploadScheduleThumbnailRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: 開催スケジュールサムネイル画像(png,jpeg形式,10MBまで)
uploadScheduleImageRequest:
  type: object
  properties:
    image:
      type: string
      format: binary
      description: 開催スケジュール蓋絵画像(png形式,10MBまで)
uploadScheduleOpeningVideoRequest:
  type: object
  properties:
    video:
      type: string
      format: binary
      description: 開催スケジュールオープニング動画(mp4形式,200MBまで)
