get:
  summary: 買い物かご取得
  operationId: v1GetCart
  tags:
  - Cart
  security:
  - cookieAuth: []
  responses:
    200:
      description: 成功
      headers:
        Set-Cookie:
          schema:
            type: string
          description: |
            Contains the session cookie named `session_id`.
            Pass this cookie back in subsequent requests.
      content:
        application/json:
          schema:
            $ref: './../../../openapi.yaml#/components/schemas/v1CartResponse'
